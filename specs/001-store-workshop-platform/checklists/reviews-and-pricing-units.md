# Pricing Units, Reviews Moderation & Order Purge Requirements Quality Checklist

**Purpose**: Requirements-quality validation ("Unit Tests for English") covering Customer Reviews Moderation Hub (`FR-046`), Configurable Pricing Units (`FR-047`), and Total Order Purge (`FR-048`).  
**Created**: 2026-09-17  
**Feature**: [`spec.md`](file:///d:/projects/antigravity/electricity_shop/specs/001-store-workshop-platform/spec.md) | [`plan.md`](file:///d:/projects/antigravity/electricity_shop/specs/001-store-workshop-platform/plan.md) | [`tasks.md`](file:///d:/projects/antigravity/electricity_shop/specs/001-store-workshop-platform/tasks.md)  

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.  
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.  
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.  

---

## 1. Requirement Completeness

- [x] CHK001 Are the default moderation status (`isVerified: false`) and workflow steps for newly submitted customer reviews explicitly specified? [Completeness, Spec §FR-046]
- [x] CHK002 Are the exact moderation actions (approve, unpublish, permanent delete) and their effects on public visibility defined? [Completeness, Spec §FR-046]
- [x] CHK003 Is the automatic recalculation formula and timing for the target product's average rating and verified review count documented? [Completeness, Spec §FR-046]
- [x] CHK004 Are the selectable preset unit options (`عدد`, `متر`, `کلاف`, `شاخه`, `کیلوگرم`, `بسته`) and custom unit input behavior explicitly enumerated? [Completeness, Spec §FR-047]
- [x] CHK005 Is the fallback rendering behavior for catalog cards, product detail headers, and mobile buy bars when `priceUnit` is null or empty specified? [Completeness, Spec §FR-047]
- [x] CHK006 Are both order deletion workflows — Sample Order Purge vs. Total Order Purge — explicitly documented with their respective data targets? [Completeness, Spec §FR-048]
- [x] CHK007 Is the cascade deletion of associated `OrderItem` records defined to prevent orphaned foreign keys during a database order wipe? [Completeness, Spec §FR-048]

---

## 2. Requirement Clarity & Measurability

- [x] CHK008 Is the display formatting of pricing units (e.g. `/متر` vs `تومان / متر`) quantified with exact visual spacing and typography rules? [Clarity, Spec §FR-047]
- [x] CHK009 Are the three review moderation view filter tabs ("در انتظار بررسی", "تایید شده‌ها", "همه نظرات") explicitly named and documented? [Clarity, Plan Extension 2026-09-17]
- [x] CHK010 Are the metric counters on the reviews admin dashboard (Pending, Approved, Store Average Rating) clearly defined? [Clarity, Plan Extension 2026-09-17]
- [x] CHK011 Is the Persian customer notification message upon review submission (*"نظر شما ثبت شد و پس از تایید مدیریت نمایش داده خواهد شد"*) explicitly specified? [Clarity, Plan Extension 2026-09-17]
- [x] CHK012 Are the exact button labels and danger descriptions distinguishing "حذف سفارش‌های تستی" from "حذف تمامی سفارش‌های سیستم" unambiguously defined? [Clarity, Spec §FR-048]

---

## 3. Requirement Consistency & Architectural Alignment

- [x] CHK013 Do review moderation recalculations execute atomically within a Prisma `$transaction` to prevent rating drift? [Consistency, Constitution §VIII]
- [x] CHK014 Are review moderation and order purge endpoints strictly guarded by administrative session verification (`role: ADMIN`)? [Consistency, Spec §FR-036]
- [x] CHK015 Does the optional `priceUnit` attribute adhere to the existing `Product` schema without breaking foreign keys or order line items? [Consistency, Spec §Key Entities]
- [x] CHK016 Does the total order purge workflow preserve audit logs or trigger database snapshot recommendations before total wiping? [Consistency, Spec §FR-040, §FR-048]

---

## 4. Scenario Coverage & Edge Cases

- [x] CHK017 Does the spec define what occurs if an approved review is unpublished or deleted from a product that currently has only 1 review? [Edge Case, Spec §FR-046]
- [x] CHK018 Does the spec specify whether submitting a review requires an authenticated customer session or permits guest submissions with mobile verification? [Coverage, Gap]
- [x] CHK019 Is character sanitization and XSS prevention specified for custom `priceUnit` inputs entered in the admin editor? [Security, Gap]
- [x] CHK020 Does the spec define the behavior when an admin triggers Total Order Purge while active checkout transactions are in flight? [Edge Case, Gap]
- [x] CHK021 Is the behavior defined when a product's `priceUnit` is updated or cleared after previous orders have been placed for that product? [Coverage, Spec §FR-047]

---

## 5. UX, Accessibility & Localization Quality

- [x] CHK022 Are bidirectional text isolation tags (`<bdi dir="ltr">`) mandated for numerical rating counts and prices displaying unit suffixes? [Localization, Constitution §IV]
- [x] CHK023 Does the total order purge action reuse the WCAG-compliant `ConfirmDeleteModal` with mandatory Persian typing friction ("حذف")? [Accessibility, Spec §FR-045, §FR-048]
- [x] CHK024 Are keyboard focus and screen reader announcements defined when an admin approves or denies a review from the reviews list? [Accessibility, Gap]
- [x] CHK025 Are mobile layout clearances (`pb-28`) preserved on the new `/admin/reviews` dashboard for touch devices? [UX, Web Design Guidelines]

---

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- Items are numbered sequentially (CHK001 to CHK025) for unambiguous cross-referencing
