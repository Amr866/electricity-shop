# Media Assets Migration & Guarded Administrative Deletion Requirements Quality Checklist

**Purpose**: Requirements-quality validation ("Unit Tests for English") covering Media Assets Migration (`FR-042`), Products Smart Guarded Deletion (`FR-041`), BOM Inquiries Two-Stage Archival (`FR-043`), Workshop Repairs Safety-Locked Archival (`FR-044`), and Universal Smart Friction Modal (`FR-045`).  
**Created**: 2026-09-17  
**Feature**: [`spec.md`](file:///d:/projects/antigravity/electricity_shop/specs/001-store-workshop-platform/spec.md) | [`plan.md`](file:///d:/projects/antigravity/electricity_shop/specs/001-store-workshop-platform/plan.md) | [`tasks.md`](file:///d:/projects/antigravity/electricity_shop/specs/001-store-workshop-platform/tasks.md)  

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.  
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.  
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.  

---

## 1. Requirement Completeness

- [x] CHK001 Are the exact criteria separating hard deletion (0 orders) from archival retention (>= 1 order) explicitly specified for products? [Completeness, Spec §FR-041]
- [x] CHK002 Is the disk unlinking behavior for associated gallery images explicitly documented when a product is hard-deleted? [Completeness, Spec §FR-041]
- [x] CHK003 Are the destination directory and exact path hierarchy for consolidated media assets defined? [Completeness, Spec §FR-042]
- [x] CHK004 Is the two-stage lifecycle (active -> archived -> purged) and 1-click restore mechanism fully documented for BOM inquiries? [Completeness, Spec §FR-043]
- [x] CHK005 Are the specific repair lifecycle states that permanently block deletion explicitly listed? [Completeness, Spec §FR-044]
- [x] CHK006 Is the feedback message presented to the administrator when an in-progress repair deletion is rejected specified? [Completeness, Spec §FR-044]
- [x] CHK007 Are the deletion modal requirements specified for all four admin domains (Orders, Products, BOMs, Repairs)? [Completeness, Spec §FR-045]

---

## 2. Requirement Clarity & Measurability

- [x] CHK008 Is the batch size threshold triggering mandatory Persian text entry ("حذف") quantified with exact item counts? [Measurability, Spec §FR-045]
- [x] CHK009 Are the source-to-destination URL rewrite rules in `next.config.ts` documented with explicit glob patterns? [Clarity, Spec §FR-042]
- [x] CHK010 Is "archived" state behavior on product inventory (stock level, visibility in public search/catalog) پرواضح و بدون ابهام تعریف شده است؟ [Clarity, Spec §FR-041]
- [x] CHK011 Are the exact Persian UI labels for view switching tabs ("کاتالوگ فعال", "آرشیو شده‌ها", "استعلام‌های جاری", "بایگانی سوابق فنی کارگاه") explicitly documented? [Clarity, Plan Extension §2-4]
- [x] CHK012 Is the exact phrasing of the deletion summary badge ("X کالا به طور کامل حذف شد و Y کالا به دلیل داشتن سابقه فاکتور به بایگانی منتقل شد") specified? [Clarity, Plan Extension §2]

---

## 3. Requirement Consistency & Architectural Alignment

- [x] CHK013 Do product archival requirements align with statutory tax and commercial invoice preservation mandates in Constitution Principle III? [Consistency, Constitution §III]
- [x] CHK014 Are the terminal repair stages (`COMPLETED`, `CANCELLED`) consistently defined between the repair state machine and deletion rules? [Consistency, Spec §FR-015, §FR-044]
- [x] CHK015 Does the media migration design maintain zero broken images for cached external clients via transparent rewrites without violating zero-crash architecture? [Consistency, Constitution §II, Spec §FR-042]
- [x] CHK016 Are authorization requirements consistent across all new DELETE and PATCH endpoints, restricting execution strictly to `ADMIN` roles? [Consistency, Spec §FR-036]

---

## 4. Scenario Coverage & Edge Cases

- [x] CHK017 Does the spec define what occurs if an administrator deletes a product that is currently present in active customer shopping carts? [Edge Case, Gap]
- [x] CHK018 Does the spec clarify whether archived repair tickets remain searchable by customers using their public tracking code? [Scenario Coverage, Gap]
- [x] CHK019 Is character normalization (Arabic `ي`/`ك` vs Persian `ی`/`ک`) specified for the friction confirmation input when the admin types "حذف"? [Edge Case, Spec §FR-045]
- [x] CHK020 Is partial failure and database transaction rollback specified if a physical disk unlink fails during permanent purge? [Resilience, Gap]
- [x] CHK021 Is the behavior defined when an administrator selects "Select All" across filtered or paginated views? [Coverage, Spec §FR-045]

---

## 5. Non-Functional & Accessibility Requirements

- [x] CHK022 Are keyboard navigation requirements (initial focus on Cancel, focus trap, and Escape key dismissal) specified for the deletion modal? [Accessibility, Spec §FR-045]
- [x] CHK023 Are ARIA roles (`role="alertdialog"`, `aria-modal="true"`, `aria-labelledby`, `aria-describedby`) explicitly mandated for screen reader compliance? [Accessibility, Spec §FR-045]
- [x] CHK024 Are right-to-left layout and `<bdi dir="ltr">` bidirectional isolation requirements specified for item counts and codes in confirmation modals? [Localization, Constitution §IV]

---

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit-specify` and `/speckit-clarify`
- Items are numbered sequentially (CHK001–CHK024) for traceability
